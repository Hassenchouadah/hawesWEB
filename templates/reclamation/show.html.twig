{% extends 'base.html.twig' %}

{% block title %}Reclamation{% endblock %}



{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.descRec }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.dateajoutrec | date("m-d-Y") }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('my') }}">Retour</a>
    {% if reclamation.traite == 0 %}

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.idRec}) }}">Modifer</a>

    {{ include('reclamation/_delete_form.html.twig') }}
    {% else %}
        reclamation traité!
    {% endif %}
{% endblock %}
