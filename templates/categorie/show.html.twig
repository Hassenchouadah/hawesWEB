{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Etoile</th>
                <td>{{ categorie.etoile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}">back to list</a>

    <a href="{{ path('app_categorie_edit', {'idHbg': categorie.idHbg}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
