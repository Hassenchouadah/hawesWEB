<form method="post" action="{{ path('app_paiement_delete', {'idpmt': paiement.idpmt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement.idpmt) }}">
    <button class="btn">Delete</button>
</form>
